import {Table} from "antd";

const RepositoryTable = () => {
    const renderTable = () => {
        return <Table>
        </Table>
    }
    return {
        renderTable,
    }
}

export default RepositoryTable;
